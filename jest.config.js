module.exports = {
  testEnvironment: 'jsdom', // Set the test environment to jsdom (for DOM-related tests)
  setupFiles: ['./geolocationMock.js'], // Add the setup file for geolocation mocking
  // Add other configuration options if needed
};

